language: node_js
branches:
  only:
    - main
    - staging
install:
  - npm ci
cache: npm
script:
  - npm run build
deploy:
  provider: npm
  email: $NPM_EMAIL
  api_key: $NPM_TOKEN
  on:
    branch: main
  skip_cleanup: true

